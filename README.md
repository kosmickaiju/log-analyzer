# log-analyzer
A simple log analyzer that allows the user to select a log file and search it for unusual and suspicious activity, such as excessive failed login attempts, suspicious IP addresses, suspicious login times, etc.
